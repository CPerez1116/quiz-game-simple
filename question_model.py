class Question:
    # Initialize a Question object
    def __init__(self, q_text, q_answer):
        self.text = q_text      # Store the question text
        self.answer = q_answer  # Store the correct answer (True/False)
